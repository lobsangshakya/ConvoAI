# ── Ollama Configuration ─────────────────────────────────────
# URL where Ollama is running (default: http://127.0.0.1:11434)
OLLAMA_BASE_URL=http://127.0.0.1:11434

# Which Ollama model to use (run `ollama list` to see available models)
OLLAMA_MODEL=qwen2.5:3b

# Timeout in seconds for Ollama requests
OLLAMA_TIMEOUT=60

# ── RAG Configuration (optional) ────────────────────────────
# Set to 1 to enable Retrieval Augmented Generation, 0 to disable
# When enabled, the bot will use files in the /knowledge folder as context
ENABLE_RAG=0

# ── Frontend ─────────────────────────────────────────────────
REACT_APP_BACKEND_URL=http://localhost:8000